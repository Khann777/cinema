import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#AF4425",
    },
    secondary: {
      main: "#662E1C",
    },
    buttons: {
      main: "#f1af18",
    },
    text: {
      main: "#C9A66B",
    },
  },
});

export default theme;
